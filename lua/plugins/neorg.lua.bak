return {
  "nvim-neorg/neorg",
  lazy = false,
  version = "*",
  config = true,
  -- require("neorg").setup({
  -- 	load = {
  -- 		["core.defaults"] = {},
  -- 		["core.concealer"] = {
  -- 			config = {
  -- 				icon_preset = "basic", --basic, diamond, varied
  -- 			},
  -- 		},
  -- 		["core.dirman"] = {
  -- 			config = {
  -- 				workspaces = {
  -- 					notes = "~/Notes",
  -- 				},
  -- 				default_workspace = "notes",
  -- 			},
  -- 		},
  -- 	},
  -- }),
}
